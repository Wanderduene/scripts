---
description: This rule ensures that the script does not delete the most recent
  kernels. The script should only delete kernels that are older than the three
  most recent ones.
alwaysApply: true
---

Ensure that the script does not delete the most recent kernels. The script should only delete kernels that are older than the three most recent ones.